{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege capability for the main window: core app/window basics plus the explicit native security commands it needs. No shell, filesystem, SQL, or URL-opening access is granted; those are added only by the task that needs them, scoped to what it needs.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "allow-session-store",
    "allow-session-load",
    "allow-session-clear",
    "allow-encrypt-value",
    "allow-decrypt-value"
  ]
}
