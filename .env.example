# OPTIONAL. The application starts and works without this file.
#
# The application origin is HARD-WIRED to https://www.tenders-sa.org and is
# never configurable: no VITE_* value, `.env` entry, or build flag can point
# the application anywhere else. The HTTP capability allow-list in
# `src-tauri/capabilities/default.json` grants exactly that one origin.
#
# Anything you DO set is validated strictly: a malformed value fails loudly at
# start-up with a screen naming the problem. The defaults remove "absent" as a
# failure mode, not "wrong".

# Authentication. Default: true.
# Set to false only to work offline against a backend that is down; the
# application then cannot sign in at all.
# VITE_FEATURE_DESKTOP_AUTH=true

# Telemetry. Defaults: disabled, strict redaction.
# VITE_TELEMETRY_ENABLED=false
# VITE_TELEMETRY_REDACTION_MODE=strict

# Request policy. Defaults: 10000 ms timeout, 2 retries on safe idempotent GETs.
# VITE_REQUEST_TIMEOUT_MS=10000
# VITE_REQUEST_MAX_SAFE_RETRIES=2

# Update metadata (public key + endpoint) lives in src-tauri/tauri.conf.json
# under `plugins.updater`; the signing private key lives only in GitHub
# secrets. There is deliberately no VITE_* updater configuration.
